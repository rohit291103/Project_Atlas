# Project Atlas — Tracker

**Last updated:** 2026-07-27 — extraction ran live on all 4 golden PRs via the **Claude Pro subscription** (no API key); a streaming-mode agent bug was found + fixed; all 12 Tier-1 checks now green

## Snapshot

Project Atlas has a scaffolded Phase 0 repo with a full industry-standard dev-tooling layer around it, and a docs tree organized to scale past Phase 0. The PRD, TRD, Roadmap, and Phase 0 Architecture are written, settled, and filed under `docs/prd/` and `docs/architecture/`; `docs/decisions/`, `docs/handoff/`, `docs/research/`, and `docs/ux/` exist as the permanent-record subfolders going forward. The `storage/` module is now complete for Phase 0: Supabase Postgres provisioned, the append-only `event_log` table implemented per TRD §3.1 (migration applied to the live instance), and `storage/projections.py` replays that log back into materialized Node/Edge state (TRD §3.2) — a pure `replay()` reducer plus a DB-backed `load_projection(workspace_id, feature_scope_id=?)`. `models/schema.py` implements the full Pydantic v2 data model (SourceRef/Node/Edge/Event, mirroring TRD §3.1 exactly) with the provenance gate enforced structurally — a Node without a SourceRef is impossible to construct, on both the write and the read (replay) path. `EventType` lives in the schema as the single source of truth; `storage/tables.py` imports it. Ingestion is real: `ingestion/github.py` is a read-only GitHub REST connector (`GitHubClient` — GET-only, Bearer auth, PR/issue/commit fetch with paginated comments + a `search_issues` primitive). All four core modules now have working code: `extraction/` implements the Claude Agent SDK agent — a pure `build_result` validation gate (raw agent payload → schema-validated Node/Edge, the one-rule-no-exceptions gate), read-only SDK tools wrapping the connector, the extraction prompt, and a `run_extraction` orchestrator that retries once then errors. The LLM call sits behind an injectable seam so the whole validate/retry flow is tested with no API key. `cli.py` now wires the pipeline end-to-end: `atlas ingest --repo owner/name --pr N` runs GitHub→extraction→`append_event` (and prints a fresh `feature_scope_id`), and `atlas review --feature-scope <id>` replays the log via `load_projection` into a Rich console report of the draft nodes (grouped by type, with confidence + status + literal source excerpt). Extraction has now run **live end-to-end on all 4 golden PRs** — authenticated through the **Claude Pro subscription** via the Claude Agent SDK's CLI login (no `ANTHROPIC_API_KEY`, no per-token cost; `Settings.anthropic_api_key` is now optional). The live run both retired the "does extraction work?" Phase 0 risk (strong, fully-provenanced output — e.g. #3472 yielded a goal/requirement/problem/architecture_note/evidence set, every node with a verbatim source excerpt) and surfaced+fixed a real bug: the SDK only invokes the `can_use_tool` safety gate in streaming-input mode, so the prompt is now passed as a one-message async stream (a string bypassed the gate and hard-errored). The 12 previously-skipped Tier-1 checks (schema/provenance/rubric) are green on all 4 PRs. What remains for Phase 0's exit criteria: the `extraction-quality-review` Tier-2 judgment pass, plus `backend-reviewer`/`security-review` on the extraction+CLI+streaming-fix code.

## Docs

### Done
- PRD, TRD, Roadmap written (`docs/prd/PRD_Product_Knowledge_Layer_MVP.md`, `docs/architecture/TRD_Context_to_Spec_Engine.md`, `docs/prd/MVP_Roadmap.md`).
- Phase 0 implementation architecture written (`docs/architecture/Phase0_Architecture.md`): Python + Claude Agent SDK agentic extraction, Supabase for storage.
- Root `CLAUDE.md` and `docs/tracker.md` established; `CLAUDE.md` now explicitly covers the entire project across all 5 phases (a "Roadmap — the whole story" section + summary table), not just Phase 0 — Phase-specific detail stays in `docs/architecture/PhaseN_Architecture.md` docs instead.
- `.claude/` agents and skills adapted from an inherited "OptiMILES" template to Atlas's actual data model, architecture, and phase.
- Eval strategy designed: new `writing-evals` skill (general LLM-eval best practices) + `extraction-quality-review` extended with a two-tier deterministic/judgment split; `tests/evals/golden_set/` structure added to `docs/architecture/Phase0_Architecture.md` §5.
- `docs/` reorganized into the six-subfolder structure (`prd/`, `architecture/`, `decisions/`, `handoff/`, `research/`, `ux/`) plus `tracker.md` as the sole top-level exception. Master docs kept their existing filenames (grandfathered) rather than renamed to the kebab-case convention; `docs/prompts/` concept replaced by `docs/handoff/` (more precise — that was its only actual use). `docs-sync` and `handoff` skills, and every doc-path reference across `.claude/` + `CLAUDE.md` + `README.md`, updated to match. 4 retroactive decision logs written to `docs/decisions/` covering everything decided so far.
- Validation repo picked (`docs/research/phase0-validation-repo-selection-v1.md`): **`BurntSushi/ripgrep`**, golden set = PRs #111, #723, #706, #3472 (optional 5th #3420). Chosen for scoped diffs (fits the ~8 tool-call budget), a gradeable domain, and decision-dense prose covering the full node vocabulary; #111's near-empty body forces `fetch_linked_issue`, #706 references a cross-repo issue. Each PR verified reachable/substantive against live GitHub 2026-07-27. Resolves the open decision carried since scaffolding (Phase0_Architecture.md §9 Q1).

### In progress
Nothing active.

### Next up
- Nothing outstanding — validation repo picked (see below); storage/Alembic decision log already written (`docs/decisions/2026-07-21-storage-engine-and-alembic-migrations.md`).

## Engineering (Phase 0 build)

### Done
- Repo scaffolded per `docs/architecture/Phase0_Architecture.md` §5: `pyproject.toml` (Python 3.12, `uv`, deps installed, `uv.lock` committed), full `src/atlas/` package layout, and `tests/` + `tests/evals/` directories. `config.py` reads `ANTHROPIC_API_KEY` / `GITHUB_TOKEN` / `SUPABASE_DB_URL` from env; `cli.py` has a working Typer `atlas` entrypoint with `ingest`/`review` stub commands (`NotImplementedError`). `.env.example` added. Note: nothing in the codebase loads `.env` into the process automatically (`uv run` does not do this) — running the CLI for real will need that wired up (`python-dotenv` is already an indirect dependency).
- Dev-tooling layer added: ruff (lint + format, `select = [E, F, I, UP, B, C4, SIM]`) and mypy (`strict = true`) configured in `pyproject.toml`; `pytest` config section added; `.pre-commit-config.yaml` (ruff, mypy, basic hygiene hooks) installed into `.git/hooks/pre-commit`; `.github/workflows/ci.yml` runs lint + format-check + mypy + pytest on every push/PR to `main`; `.editorconfig` added; `.python-version` pinned to 3.12. Verified end-to-end: `ruff check`, `ruff format --check`, `mypy src`, `pytest`, and `atlas --help` all pass clean.
- Supabase project provisioned (free tier, hosted Postgres); `SUPABASE_DB_URL` set in local `.env` (gitignored, never committed).
- `storage/db.py`: SQLAlchemy 2.0 engine/session (`get_engine`, `get_sessionmaker`, `session_scope`), `Base` declarative base. `normalize_database_url()` rewrites Supabase's bare `postgresql://` connection strings to `postgresql+psycopg://` (psycopg v3 is the installed driver, not psycopg2 — without the rewrite, connecting fails with `ModuleNotFoundError`). `pool_pre_ping=True` guards against stale hosted-Postgres connections.
- `storage/tables.py`: the append-only `event_log` table (TRD §3.1 Event shape exactly — `id`, `event_type`, `payload` JSON/JSONB, `actor`, `timestamp`, `workspace_id`) plus `append_event()` as the sole write path. No update/delete helper exists anywhere in the module (enforced by convention + a test asserting neither symbol exists). `EventType` enum column uses `values_callable` so Postgres stores the lowercase TRD values, not Python's uppercase member names — a real bug caught by testing against live Postgres (SQLite has no native enum constraint and silently accepted the mismatch).
- Alembic wired up and run for real: `alembic.ini` (connection string deliberately left blank — `migrations/env.py` reads `SUPABASE_DB_URL` from the environment at runtime, never committed), initial migration `create_event_log` applied to the live Supabase instance via `alembic upgrade head` and verified by inspecting the resulting table + a real write/read/delete round-trip. `id` has a DB-level default (`gen_random_uuid()`) in addition to the ORM-level one, so inserts that bypass the ORM still get a valid id.
- `tests/test_storage.py`: 9 tests covering table shape, `append_event` write/read round-trip, transactional rollback on error, the enum value-vs-name storage regression, and `normalize_database_url`'s URL-rewrite behavior (bare scheme, explicit driver, non-Postgres schemes) — all against in-memory SQLite, no live DB required in CI.
- Storage reviewed via `security-review` (no findings — no user input reaches SQL, no secret logged or committed, DB URL is env-only) and `backend-reviewer` (no blocking issues; see Next up for the open `workspace_id` design question it raised).
- `models/schema.py`: full Pydantic v2 core data model (TRD §3.1 mirror, field-for-field) — `SourceRef`, `Node`, `Edge`, `Event` on a shared `AtlasModel` base with `extra="forbid"` (rejects hallucinated extraction fields), plus all six enums (`SourceType`, `NodeType`, `NodeStatus`, `CreatedBy`, `RelationType`, `EventType`). The non-negotiable provenance rule is structural: `Node.source_refs` requires ≥1 entry and `SourceRef.excerpt`/`url` reject blank/whitespace, so a Node without literal provenance cannot be constructed. `confidence_score` bounded [0.0, 1.0] on Node+Edge; `status` defaults to `unconfirmed`; excerpts preserved verbatim (never stripped) for provenance verification; Edge rejects self-references. Built test-first per `tdd`; `tests/test_schema.py` has 37 tests weighted toward failure cases (empty provenance, out-of-range confidence, unknown enums, `extra="forbid"` on every entity, JSON round-trip for event-payload storage). Reviewed via `backend-reviewer` — clean, confirmed faithful TRD mirror and sound provenance gate.
- `EventType` unified: moved from `storage/tables.py` into `models/schema.py` (schema is the single source of truth); `storage/tables.py` now imports it (storage→models dependency direction). Kills the duplicate-enum drift risk. `alembic check` confirms zero DB schema drift from the change; all storage tests still green.
- `workspace_id` source decided (`docs/decisions/2026-07-21-phase0-default-workspace-id.md`): `config.DEFAULT_WORKSPACE_ID = uuid.UUID(int=0)` (nil sentinel), stamped by application code, with the schema field kept *required* (not defaulted) so the single-workspace assumption stays visible at call sites and disappears cleanly in Phase 4. `tests/test_config.py` guards its stability. Unblocks projections/ingestion/extraction.
- `storage/projections.py`: event-log replay → materialized Node/Edge state (TRD §3.2), built test-first per `tdd` (`docs/decisions/2026-07-22-projections-event-replay.md`). Pure `replay(events) -> Projection` fold (structural `Protocol` over both `EventLog` rows and `Event` models) + thin `load_projection(session, *, workspace_id, feature_scope_id=None)`. Re-validates every `node_created`/`edge_created` payload through the Pydantic gate on read (provenance holds on the read path too); skips audit-only `ingestion_run`/`spec_exported`; raises `NotImplementedError` on the Phase 1 `node_confirmed/edited/rejected` events rather than silently mishandling them; exhaustive `else` guards a new EventType with no handler. `Projection.for_feature_scope()` filters nodes by scope and drops edges with an endpoint outside it. `tests/test_projections.py`: 16 tests (pure replay, Phase 1 fail-loud, read-path provenance revalidation, scope filtering, DB round-trip incl. workspace + feature-scope filtering).
- `storage/tables.py` UUID columns made dialect-portable — `PG_UUID(as_uuid=True).with_variant(Uuid(as_uuid=True), "sqlite")` (mirrors the existing JSON/JSONB variant). Fixes a real SQLite-only defect surfaced by the projection tests: the nil `DEFAULT_WORKSPACE_ID` (`"000…0"`, all digits) got NUMERIC affinity under the bare `UUID` type name, coercing to integer `0` and crashing on read. Postgres DDL byte-identical (verified offline via `CreateTable` compile) — zero migration drift; only SQLite emission changes to `CHAR(32)`.
- `ingestion/github.py`: read-only GitHub REST connector (`docs/decisions/2026-07-22-github-ingestion-connector.md`). `GitHubClient` (httpx-backed, Bearer auth, `X-GitHub-Api-Version` pinned) exposes `fetch_pull_request` (title/body/state + full paginated conversation comments), `fetch_issue`, `fetch_commit`. Owns *all* GitHub HTTP + JSON parsing so the later `extraction/tools.py` agent tools wrap it thinly and the read-only guarantee lives in one place. Returns transient frozen-dataclass DTOs (`PullRequest`/`Issue`/`Commit`/`IssueComment`, TRD §4.2 — not domain Nodes), each carrying the provenance handles a SourceRef needs (number/sha, html url, literal text). `GitHubError` surfaces failures and flags rate-limit (403/429 + `X-RateLimit-Remaining: 0`) rather than auto-retrying. `tests/test_github.py`: 10 tests via `httpx.MockTransport` replay of `tests/fixtures/github/*.json` (parsing incl. nested commit author/message, `extra`-field tolerance, comment pagination, 404 + rate-limit errors, a **GET-only** read-only assertion, and Bearer/API-version headers). No live API/token/network in tests. Inline security cross-check clean (GET-only test-enforced, token header-only never logged, no persistence).
- `ingestion/github.py` `search_issues`: read-only `GET /search/issues` primitive added when the extraction `search_repo` tool needed it (reuses the issue parser; scoped per-repo by the tool). Still deferred in the connector (not built ahead): incremental sync / `last_synced_at` (Phase 1), auto-backoff/retry.
- `extraction/` (Claude Agent SDK) — all three files + the validation gate (`docs/decisions/2026-07-23-extraction-agent.md`). `agent.build_result` is the pure schema-validation gate: raw agent payload → domain Node/Edge or raise (rejects missing/blank provenance, unknown enum, hallucinated field, duplicate node ref, dangling/self edge), assigning UUIDs, wiring edges via the agent's local `ref` ids, mapping low/medium/high confidence to 0.3/0.6/0.9, stamping workspace/feature scope. `run_extraction` orchestrates via an injectable `AgentCall` seam (LLM call isolated) — validate, retry once with the error fed back, then `ExtractionError`. `tools.py`: read-only SDK tools (`fetch_linked_issue`/`fetch_commit`/`search_repo` wrapping `GitHubClient`, `emit_extraction` as the forced output tool). `prompts.py`: extraction system prompt + seed-prompt builder. Agent locked down by a single `can_use_tool` gate (the authority): allows only the 4 atlas tools, always permits `emit_extraction`, and **counts fetch/search calls to enforce the ~8-call cost cap** (Phase0 §2) — deeper than `max_turns`, which can't cap parallel calls in one turn. Built-ins hard-denied via `disallowed_tools`, `setting_sources=[]`, no `allowed_tools`/`bypassPermissions`. Tool handlers surface httpx transport errors (not just `GitHubError`) as error results and offload the blocking sync connector via `asyncio.to_thread` so they don't stall the SDK loop. `tests/test_extraction.py`: 24 tests (confidence map, gate happy + failure cases, transcript parsing, retry-once flow via a fake agent, tool formatting + transport-error via `httpx.MockTransport`, permission-gate cap/allow/deny) — **LLM never called, no key needed**. Introspected the installed `claude-agent-sdk` (0.2.116) for its real API rather than guessing.
- `cli.py` wired end-to-end (`atlas ingest`/`review`), connecting all four modules. `ingest --repo owner/name --pr N`: `load_dotenv()` → `Settings.from_env()` → `GitHubClient` → `extract_from_pull_request(...)` (fresh `uuid4` feature_scope_id per run) → `record_extraction` writes `node_created`/`edge_created` events via `append_event` → prints the feature_scope_id. `review --feature-scope <id>`: `load_projection(...)` → `render_projection` Rich report (nodes grouped by type, showing content/confidence/status/`url`+verbatim excerpt, plus edges). `review` needs only `SUPABASE_DB_URL` (not the ANTHROPIC key), so it works before keys land. The report gives each node a short-id column and references those same short ids in the Edges table so a reviewer can map an edge to its endpoints; `ingest` catches `GitHubError`/`ExtractionError` and prints a clean message instead of a traceback. Reusable pieces (`_parse_repo`, `_short_id`, `record_extraction`, `render_projection`) factored out of the command bodies; `tests/test_cli.py`: 8 tests incl. a full persist→replay→render round trip through in-memory SQLite (no LLM, no live DB) and an edge-mappability assertion. `atlas --help` verified. Command bodies (live GitHub/agent/Postgres) intentionally not unit-tested (lighter-touch CLI wiring per `tdd`).
- `feature_scope_id` source resolved: each `atlas ingest` run mints a fresh `uuid4`, surfaced to the user, and consumed by `atlas review --feature-scope <id>`. No sentinel needed (unlike workspace_id).
- Read-only `GITHUB_TOKEN` (fine-grained PAT, "Public Repositories (read-only)") set in `.env` and verified live: fetched ripgrep PR #111 + followed its link to issue #109 through `GitHubClient` — the whole ingestion path works against the real repo, zero Anthropic spend.
- Golden-set eval harness built (`tests/evals/`, needs only the GitHub token). `golden_set/pr-{111,723,706,3472}/raw.json` = the exact corpus the connector captures (PR body + conversation comments + body-linked same-repo issues), recorded live. Per-PR `rubric.yaml` = conservative hand-authored Tier-1 floor (required node types + must-mention keywords; type-ambiguous claims pinned via keyword, not type). `test_golden_set.py`: pure deterministic checkers (corpus assembly, whitespace-tolerant provenance-excerpt verification, rubric floor, schema-gate on read) unit-tested with hand-built inputs (`tdd`), plus parametrized golden-dir tests — fixture-wellformedness runs now; the three extraction-dependent checks (schema/provenance/rubric) `skip` until a live run drops `extraction.json` (contract: `{"nodes":[…model_dump],"edges":[…]}`) into each dir, kept as a replayable fixture so re-runs need no key (Phase0 §5). `pyyaml`+`types-pyyaml` added as dev deps. 16 new tests pass (122 total, 12 skipped). Selection rationale + the honest connector-scope caveat recorded in `docs/research/phase0-validation-repo-selection-v1.md`.
- Extraction ran **live end-to-end on all 4 golden PRs**, authenticated via the **Claude Pro subscription** (Claude Agent SDK → Claude Code CLI login), no `ANTHROPIC_API_KEY`, zero per-token cost. `Settings.anthropic_api_key` made optional (`os.environ.get`, `str | None`) — the SDK handles auth via the CLI login; setting the env var instead routes through pay-as-you-go API billing (a separate wallet from the subscription). Each run's output saved as `tests/evals/golden_set/pr-<n>/extraction.json`; the 12 previously-skipped Tier-1 checks (schema/provenance/rubric) now **pass on all 4 PRs** — zero fabricated excerpts anywhere. Output quality is strong for Phase 0 (e.g. #3472 → goal/requirement/problem/architecture_note/evidence, every node verbatim-provenanced; 9–11 nodes on the richer PRs). 135 tests total, mypy + ruff clean.
- Bug found + fixed (extraction, would've been Critical — the agent had never run live): the Claude Agent SDK only invokes the `can_use_tool` permission gate (our read-only + ~8-call-budget enforcement) in **streaming-input mode**; a plain-string prompt bypasses the gate and the current SDK hard-errors on it. `_make_agent_call` now wraps the prompt as a one-message async stream (`_as_stream`, in the SDK's exact stream-json user-message shape). Regression test added (`test_as_stream_yields_single_streaming_user_message`) locking the wire shape so it can't silently revert to a string.
- Finding (connector scope, not a bug): the Phase 0 connector fetches PR body + *conversation* comments + linked issues, but NOT inline *review* comments — so review-thread design discussion (e.g. #3472's edge-case debate) is invisible to extraction. Golden corpora + rubrics were authored against only what the connector actually captures, keeping the eval honest. Worth a connector enhancement decision if review-comment context proves important post-first-run.
- Finding (CI hygiene gap): CI and the pre-commit hook run `mypy src` only, so `tests/` escapes strict type-checking — `tests/test_extraction.py` + `tests/test_cli.py` (from the extraction/CLI session) currently have 9 `mypy --strict` errors that neither gate catches. Not a runtime problem (all 122 tests pass); worth either widening the mypy target to `tests/` or fixing those files. Left untouched here (out of this task's scope; user deprioritized reviewing that code).
- Deliberately not added yet (would be premature at this stage): LICENSE (commercial product, undecided), Makefile, Dependabot/renovate, issue/PR templates, CODEOWNERS, Docker/devcontainer (contradicts the no-local-container decision already made for Supabase).

### In progress
Nothing active.

### Next up
- **The ANTHROPIC-key blocker is gone** — live extraction runs on the Claude Pro subscription. Tier-1 is green on all 4 PRs. The remaining Phase 0 exit work is the judgment pass: run `extraction-quality-review` for the Tier-2 pass (content correctness, completeness, calibration) against the Phase 0 exit criteria — at N=4 this is qualitative signal, not a pass rate (`writing-evals`). Together with the now-green Tier 1, this *is* Phase 0's exit criterion ("usable, mostly-correct structured output on 3–5 real PRs").
- Run `backend-reviewer` (extraction + CLI) and the formal `security-review` on the extraction/CLI work **including the streaming-mode fix** (verify the `can_use_tool` gate actually enforced read-only + the ~8-call cap during the live runs — now testable in practice, was unverified before). Not yet done.
- Commit the session's work: the streaming fix + `_as_stream` regression test, the optional-`anthropic_api_key` config change, the 4 `extraction.json` golden fixtures, and this tracker update (plus still-uncommitted prior work — extraction module, CLI, golden set, research doc). Throwaway recorder lives in scratchpad (`run_extraction.py`), not committed — fold into an `atlas` eval-record command later if wanted.
- Forward note (Phase 1, not now): `Node.confidence_score` is currently required. When Phase 1's confirmation UI adds manual node creation (`created_by=user`), TRD §6 says those skip confidence scoring — the field will need to become `float | None` (or a validator requiring it only when `created_by=system`). Log a decision then, not now.
- Forward note (Phase 1, not now): projection replay raises `NotImplementedError` on `node_confirmed/edited/rejected`; add handlers + a monotonic sequence column (replay order becomes load-bearing) when the confirmation UI lands. See `docs/decisions/2026-07-22-projections-event-replay.md`.

## Last session notes

Started from the user's cost question ("I already have $20 Pro — why pay for an API key too?"). Clarified the two-wallets reality (Claude Pro ≠ Anthropic API), then realized the extraction agent runs on the **Claude Agent SDK**, which authenticates via the **Claude Code CLI login** — so the Pro subscription covers it with no API key. Made `Settings.anthropic_api_key` optional to unblock that path, then ran a real extraction to verify. It worked (no key, no per-token cost) **and surfaced a real bug**: the SDK only fires the `can_use_tool` safety gate in streaming-input mode; our string prompt bypassed it and hard-errored. Fixed by wrapping the prompt as a one-message async stream (`_as_stream`) + added a regression test. Then ran **all 4 golden PRs live** on the subscription, saved each `extraction.json`, and the 12 previously-skipped Tier-1 checks (schema/**provenance**/rubric) now pass on every PR — **zero fabricated excerpts**. Extraction quality is genuinely strong for Phase 0 (verbatim provenance on every node; sensible types + edges; 5–11 nodes/PR). **135 tests pass**; mypy + ruff clean. **This effectively retires Phase 0's core risk ("does extraction work?") on the Tier-1/deterministic axis.** Remaining for exit criteria: the `extraction-quality-review` Tier-2 human judgment pass, and `backend-reviewer`/`security-review` on the extraction+CLI+streaming-fix (which can now verify the read-only gate + call cap held in practice). **Uncommitted at session end:** the streaming fix + regression test, the config change, the 4 `extraction.json` fixtures, this tracker — plus the still-uncommitted prior extraction module/CLI/golden-set/research work. (Note: `tests/` still escapes CI's `mypy src` — 9 pre-existing strict errors in `test_extraction.py`/`test_cli.py`, unrelated to this session's additions, still uncaught.)
