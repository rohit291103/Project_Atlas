# Research

External findings that inform Project Atlas but aren't product requirements or architecture themselves — e.g. validation-repo candidate notes, extraction-quality run logs (per the `extraction-quality-review` skill), competitive landscape.

Naming: `kebab-case-topic-v1.md` (bump `v2`, `v3`, … on major revision; never overwrite — a research doc is a record of what was true when it was written). Nothing here yet — this file exists so the folder is tracked before its first real doc.
