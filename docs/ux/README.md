# UX

Design-system baseline and page/flow specs for Project Atlas's confirmation UI — doesn't start until Phase 1 (`docs/prd/MVP_Roadmap.md`, Weeks 5–9). Reviewed by the `frontend-reviewer` agent once it exists.

Naming: `kebab-case-topic-v1.md`. Nothing here yet — this file exists so the folder is tracked before Phase 1 work begins.
