# Handoff notes

Structured handoff notes for another collaborator (a teammate, another AI tool, or a future Claude session picking this back up), produced by the `handoff` skill (`.claude/skills/handoff/SKILL.md`).

Naming: `YYYY-MM-DD-short-slug.md`. Nothing here yet — this file exists so the folder is tracked before its first real note.
